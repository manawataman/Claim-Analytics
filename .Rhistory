library(tidyverse)
library(stringr)
library(forcats)
library(caTools)
library(DT)
#install.packages("data.table")
library(data.table)
#install.packages("pander")
library(pander)
library(ggplot2)
library(scales)
library(grid)
library(gridExtra)
library(corrplot)
#install.packages("VIM")
library(VIM)
library(knitr)
library(vcd)
#install.packages("caret")
library(caret)
library(xgboost)
library(MLmetrics)
library('randomForest')
library('rpart')
#install.packages("rpart.plot")
library('rpart.plot')
library('car')
library('e1071')
library(vcd)
library(ROCR)
#install.packages("pROC")
library(pROC)
library(VIM)
#install.packages("glmnet")
library(glmnet)
library(zipcode)
claimsTrain <- read.csv("C:/Users/aman manawat/Documents/Kaggle/Travelers/dataset/uconn_comp_2018_train.csv")
claimsTest <- read.csv("C:/Users/aman manawat/Documents/Kaggle/Travelers/dataset/uconn_comp_2018_test.csv")
head(claimsTest)
summary(claimsTrain)
str(claimsTrain)
head(claims)
library(caTools)
set.seed(123)
split = sample.split(claims$fraud,SplitRatio = 0.75)
claims_training_set <- subset(claims,split == TRUE)
claims_test_set <- subset(claims, split == FALSE)
split = sample.split(claims$fraud,SplitRatio = 0.75)
claims_training_set <- subset(claims,split == TRUE)
claims_test_set <- subset(claims, split == FALSE)
claims <- data.frame(claimsTrain)
claims <- data.frame(claimsTrain,claimsTest)
claims <- data.frame(c(claimsTrain,claimsTest))
claims <- (c(claimsTrain,claimsTest)
claims <- c(claimsTrain,claimsTest)
claims <- c(claimsTrain,claimsTest)
claims <- data.frame(claims)
head(claims)
head(claims,2)
head(claims,1)
claims <- claimsTrain + claimsTest
claims <- claimsTrain, claimsTest
claims <- rbind(claimsTrain,claimsTest)
claimsTest$fraud <- NULL
claims <- rbind(claimsTrain,claimsTest)
head(claimsTest)
summary(claimsTrain)
str(claimsTrain)
dim(claimsTrain)
dim(claimsTest)
str(claimsTest)
claimsTest$fraud <- NULL
fraud <- NULL
claimsTest <- cbind(claimsTest,fraud)
claimsTest <- cbind(claimsTest,fraud <- NULL)
claimsTest$fraud <- NULL
str(claimsTest)
claimsTest$fraud <- NULL
claimsTest$fraud
claimsTest
for(val in claimsTest)
{
claimsTest <- cbind(claimsTest,fraud)
}
claimsTest <- cbind(claimsTest,c(fraud))
claimsTest <- cbind(claimsTest,data.frame(fraud))
claimsTest <- cbind(claimsTest,data.frame(fraud<-NULL))
claimsTest['fraud'] <- NULL
head(claimsTest)
str(claimsTest)
claimsTest <- read.csv("C:/Users/aman manawat/Documents/Kaggle/Travelers/dataset/uconn_comp_2018_test.csv")
claimsTest['fraud'] <- NULL
str(claimsTest)
